from setuptools import setup

setup(name="cmctools",
      version="0.1",
      description="A simple wrapper + tools for the coinmarketcap.com API.",
      author="Dustin Williams",
      packages=["cmctools"])
